node_modules/
dist/
dev-dist/
*.local

# Supabase anahtarlari — bkz. .env.example. `anon` anahtari gizli degil
# ama gelistirme verisi gercek olculere karismasin diye depoda tutulmuyor.
.env.local
.DS_Store

# macOS'un exFAT/ag suruculerde biraktigi AppleDouble golgeleri. Git bunlari
# .git/objects icinde gercek pack sanip "non-monotonic index" hatasi veriyor.
._*

# Gorsel uretim klasoru — ciktilar import:images ile src/assets/cards altina gecer
gorseller/
gorsel-brief.tsv

# Capacitor Android projesi — cap add android ile yeniden uretilir
android/

# Elenen logo adaylari — 21 MB, depoya girmesin (diskte duruyor)
brand/denemeler/

# Ham havuz disa aktarimi — tek kaynak kart-havuzu-300.xlsx, bu ondan turuyor
/cards.json

# Release imzasi — keystore KAYBOLURSA uygulama bir daha guncellenemez.
# Depoya girmez; yedegi ayri bir yerde, parolayla birlikte durmali.
android-imza.properties
*.jks
*.keystore
